# Paste these root-level values into Streamlit Community Cloud → App settings → Secrets.
# Root-level Streamlit secrets are also exposed as environment variables.
RAGIFY_BACKEND_URL = "https://ragify-rxi9.onrender.com"

# Leave empty while the backend API-key store is in open mode.
RAGIFY_API_KEY = ""
